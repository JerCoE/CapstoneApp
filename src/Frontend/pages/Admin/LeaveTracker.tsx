import React from 'react';

export default function LeaveTracker(): JSX.Element {
  return (
    <section style={{ padding: 24 }}>
      <h2>Leave Tracker (Admin)</h2>
      <p>This is the admin leave tracker component placeholder. Replace with your planned component implementation.</p>
    </section>
  );
}